import React from 'react';
import PropTypes from 'prop-types';
import "./table-component.scss"

const TabletComponent = (props, context) => {
  return (
    <div>
        <h1>Tablet Component</h1>
    </div>
  );
};

export default TabletComponent;

TabletComponent.propTypes = {};
